[package]
name = "arna-agent"
version = "0.1.0"
edition = "2021"
description = "Arna agent: screen capture (Phase 1c)"
license = "LicenseRef-Proprietary"

[dependencies]
scrap = "0.5"
image = { version = "0.25", default-features = false, features = ["jpeg"] }
