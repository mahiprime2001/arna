[package]
name = "arna-poc"
version = "0.1.0"
edition = "2021"
description = "Arna WebRTC P2P proof-of-concept CLI (Phase 1b)"
license = "LicenseRef-Proprietary"

[dependencies]
arna-core = { path = "../core" }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
